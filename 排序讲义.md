1. 引入

   排序算法指的是将一组数据按照某种顺序进行排列的算法。

   衡量一个排序算法性能的标准：
   时间复杂度，空间复杂度，稳定性。其中，稳定性是相同的元素在排序后相对顺序是否发生了改变。

2. 冒泡排序

   冒泡排序是最简单的排序算法之一。我们可以根据冒泡排序来学习排序算法共通的特性。

   冒泡排序的原理特别简单。我们以升序排序为例，从第一个元素开始，每次检查两个相邻的元素，如果前面的元素大于后面的元素，就交换这两个元素。当没有元素需要交换时，这次排序就完成了。

   由于我们一共有 $n$ 个元素，所以要对除了最后一个元素之外的 $n - 1$ 个元素都做一次扫描。

   伪代码：

   ```Pseudocode
   Input. An Array A consisting of n elements.
   Output. A will be sorted in nondecreasing order stably.
   Method.
   need_sort <- True
   while need_sort
   	need_sort <- false
   	for i <- 1 to n - 1
   	if A[i] > A[i + 1]
   		need_sort <- true
   		Swap A[i] and A[i + 1]
   ```

   时间复杂度：最坏情况下我们需要对 $n - 1$ 个元素做完整的扫描。每次扫描都需要与后面的元素交换。也就是说，如果给一个**完全逆序**的数组排序，需要交换 $(n - 1) + (n - 2) + (n - 3) + \cdots + 3 + 2 + 1 = \frac{n(n - 1)}{2}$。我们记时间复杂度为 $O(n^2)$。

   观察上面的代码，由于我们在动态的调整下一次是否需要排序，所以除非遇到完全逆序的数组，一般是不会严格达到 $O(n^2)$ 的量级的。实际上很多人的冒泡排序写法是这样的：

   ```Pseudocode
   Input. An Array A consisting of n elements.
   Output. A will be sorted in nondecreasing order stably.
   Method.
   for i <- 1 to n
   	for j <- 1 to n - i
   	if A[j] > A[j + 1]
   		Swap A[j] and A[j + 1]
   ```

   也就是强制扫描所有的元素，此时时间复杂度就是严格 $O(n^2)$ 了。

   空间复杂度：由于我们没有使用额外的数组，因此空间复杂度为 $O(1)$。

   稳定性：由于我们只有相邻元素严格逆序的情况下才交换，因此冒泡排序是稳定的。

   A2096（要求学生根据伪代码自行写出代码）

   由于冒泡排序的时间复杂度过高，在算法竞赛中它一般是不实用的。我们不会真的将一道题中的排序部分交给冒泡排序。但是冒泡排序的思想可以用于一些题目。C1014

   思考：冒泡排序的核心是什么？

   显然，冒泡排序的核心是**交换**。事实上，交换是很多排序算法的核心。

3. 快速排序

   快速排序依然是一种依赖交换的排序算法。它使用**分治**的思想解决问题。具体来说，快速排序的过程分为三部分：

   - 选出一个基准数
   - 将数组分为两部分，小于等于基准点的数，放到基准点左边；大于基准点的数，放到基准点右边。
   - 递归左边的数组，重复执行步骤；递归右边的数组，重复执行步骤。

   对于基准点的选择。最好的基准点显然是中位数，但是找到数组的中位数需要额外的时间开销。所以找基准点一般有两种做法：第一种做法是随机选择一个数；第二种是使用线性找中位数的算法找到中位数。我们使用第一种做法。

   快速排序的**最优**时间复杂度为 $O(n \log n)$ **平均**时间复杂度是 $O(n \log n)$，然而，如果待排序数组是一个由完全重复的数字构成的数组，快速排序的时间复杂度会退化到 $O(n^2)$。

   在算法竞赛中，我们一般只考虑复杂度的最坏情况。所以 $O(n^2)$ 的朴素快排是无法满足要求的。

   下面介绍一种可行的优化：每次分出基准点后将数组分为三部分：左边小于基准点的数；中间等于基准点的数；右边大于基准点的数。这样每次就可以把相同的数聚集到一起。优化后最优时间复杂度为 $O(n)$，平均时间复杂度为 $O(n \log n)$。虽然最坏时间复杂度依旧是 $O(n^2)$，但是一般情况下很难构造出这样的数据。

   ```Pseudocode
   Input. An Array A consisting of n elements.
   Output. A will be sorted in nondecreasing order stably.
   function quick_sort (Arr, len)
   	if (len <= 1) return
   	pivot <- Arr[rand() % len]
   	i <- 0, j <- 0, k <- len
   	while i < k
   		if Arr[i] < pivot 
   			Swap Arr[i] and Arr[j]
   			i <- i + 1
   			j <- j + 1
   		else if pivot < Arr[i]
   			k <- k - 1
   			Swap Arr[i] and Arr[k]
   		else
   			i++
   	quick_sort(Arr, j)
   	quick_sort(Arr + k, len - k)
   ```

​		P1124（学生根据伪代码自行实现）

​		快速排序的空间复杂度取决于递归的深度，记作 $O(\log n)$。由于我们是随机选择基准点然后划分，所以快速排序是不稳定的。

​		在 C++ 中，大家使用的 std::sort() 其实也是快速排序，严格来说，它是叫做**内省排序**，是快速排序进一步的优化。做到了严格意义		上最坏时间复杂度 $O(n \log n)$。

4. 归并排序

   归并排序也是一个基于分治思想的排序。它的排序过程如下：

   - 分解：将当前的数组从中间分开，分成两个子数组。此时，如果数组被分解成长度为1，那么这个子树组一定是有序的；
   - 解决：检查出来分解出来的数组是否有序，如果有序就将它们合并（步骤三）为一个有序的数组。否则不断的递归，执行分解（步骤一） - 判断（步骤二） - 合并的过程;
   - 合并：将有序的子树组合并为一个大数组。
   
   ```Pseudocode
   Input. An Array A consisting of n elements.
   Output. A will be sorted in nondecreasing order stably.
   function merge_sort (Arr, l, r)
   	if l >= r
   		return;
   	mid <- (l + r) / 2;
   	merge_sort(Arr, l, mid)
   	merge_sort(Arr, mid + 1, r)
   	tmp = new array of size (n)
   	k <- 1
   	l <- 1
   	j <- mid + 1
   	while i <= mid and j <= r
   		if arr[i] <= arr[j]
   			tmp[k] = arr[i]
   			i <- i + 1
   		else
   			tmp[k] = arr[j]
   			j <- j + 1
   		k <- j + 1
   	while i <= mid
   		tmp[k] = arr[i]
   		i <- i + 1
   		k <- k + 1
   	while j <= r
   		tmp = arr[j]
   		j <- j + 1
   		k <- k + 1
   	for i <- l, j <- 1 to r
       arr[i] <- tmp[j]
       i <- i + 1
       j <- j + 1
   ```
   
   P1126（要求学生根据伪代码自行写出代码）
   
   归并排序的最坏时间复杂度是 $O(n \log n)$。
   
   由于归并排序使用了一个辅助数组 $tmp$，所以它的空间复杂度是 $O(n)$。归并排序是稳定的。
   
   除了排序外，归并排序的一个常用做法是求**逆序对**。
   
   由于归并排序每次递归会将数组分成左右两个部分，左边的数组优先进行排序。此时我们对左边的数组做一个遍历，将它们与右边的数组比较。如果当前的元素和右边的某个位置是逆序对，由于左边是有序的，因此对于左边这部分数组，从当前位置开始直到结束的所有元素都和右边是逆序对，因此会产生 $mid− i + 1$的贡献。
   
   C1105（学生自行思考五分钟）
   
   在C++中其实也存在一个归并排序函数，它就是 std::stable_sort()。即“稳定的排序”。使用方法和 std::sort() 完全一样。
   
5. 结论

   在实际的算法竞赛中。大家往往只需要掌握 std::sort() 和 std::stable_sort() 的用法就可以了。但是各种排序算法的思想非常重要。理解它们能够帮我们解决很多问题。

   作业：YL1040E4

   G1125