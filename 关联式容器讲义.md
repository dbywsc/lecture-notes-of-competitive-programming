1. 引入

   （如果学生不会迭代器的概念，先讲迭代器）
   
   给定若干个数，如何判断在这些数字中，$x$ 有没有出现过 / $x$ 出现的次数？
   
   容易想到建立桶数组，设 $book_x$ 为 $x$出现的次数。但是这样做会造成很大的空间浪费。（可以拓展离散化的原理）
   
   如果要判断的数字非常大、判断的数字是负数、判断的元素不是一个数字应该怎么办？
   
   可以使用**关联式容器**。
   
2. std::map

​		map 的定义形如 map<int, int> M;使用上类似于数组。

​		map 内部的键值对是 **有序** 的。

​		判断 map 中是否存在一个first为x的键值对：M.count(x)	输出 map 中某个元素 $x$ 的second：	M[x]（注意，如果不存在first为 $x$ 		的键值对会自动插入一个 {x, 0}）

​		其中，形如 M[x]++ 的操作时间复杂度是 $O(\log n)$，M.count(x) 的时间复杂度是 $O(\log n)$，其中 $n$ 是当前 map 中存储的元素个数。

​		虽然 map 不是线性的，但是在 C++11 之后，可以做形如

```cpp
for (auto i : M) {
  cout << i.first << ' ' << i.second << '\n';
}
```

​		的遍历。

​		A4096 A4094（课上讲解）

​		A4097 （学生自己做 10分钟）

3. std::set std::multiset

   set 的定义形如 set\<int> S；

   ​	set 内部的元素是 **有序且去重的**。

   判断 set 中是否存在一个元素：S.count(x) 

   在 set 中加入一个元素: S.insert(x)

   在 set 中删除一个元素: S.erase(x)

   其中，形如 S.insert(x) 操作的时间复杂度是 $O(\log n)$， S.count(x) 的平均时间复杂度是 $O(\log n)$，S.erase(x) 的平均时间复杂度是 $O(\log n)$，其中 $n$ 是当前 set 中存储的元素个数。

   虽然 set 不是线性的，但是在 C++11 之后，可以做形如

   ```cpp
   for (auto i : S) {
     cout << i << '\n';
   }
   ```

   的遍历。

   multiset 的定义形如 multiset\<int> S;

   multiset 与 set 相比，**去掉了去重操作**。

   此时的 S.count(x) 操作返回多重集中的 $x$ 的个数（要注意，最坏情况下 count 操作的时间复杂度会来到 $O(n)$ ）。

   Multiset 的 S.erase(x) 操作，如果 $x$ 是一个**迭代器**，那么只会删除当前的迭代器所指的元素，如果 $x$ 是一个元素，那么会删除**所有等于 $x$ 的元素**。

   其它完全一致。

   A4093 A4100 G1177

   ABC344C（学生自己做 10 分钟）

   ABC170C （学生自己做 5 分钟）

4. set 的二分操作

   std::set 和 std::multiset 支持二分操作

   形如 S.lower_bound(x) 和 S.upper_bound(x) ，时间复杂度 $O(\log n)$

   A4095

5. 容器之间的嵌套

   ABC278C

6. 总结

   关联式容器的常见用法：

   用于为特定条件计数、判断特定条件是否出现过、预处理可能会出现的条件。

7. 作业

   G1044

   CF1426D

   ABC233D



