1. 引入

​	P1468 查找

​	形式化题意：给一个长度为 $n$ 并且 **单调不减** 的数组，接下来有 $q$ 次询问。每次询问一个数 $x$ ，要求输出这个数字第一次在数组中出	现的位置，如果没有出现过输出 $-1$。

​	考虑最暴力的做法，对数组做一个从 $1$ 到 $n$ 的遍历，如果当前的 $a_i = x$，那么 $i$ 一定是第一次出现 $x$ 的位置。

​	然而，这种做法单次时间复杂度是 $O(n)$，总共时间复杂度是 $O(qn)$，无法通过本题。

​	接着思考，是否还存在别的做法？（学生自行思考两分钟，如果学生提出了使用map或者set等容器的做法，在讲解完二分后一起分	析时间复杂度）。

​	在我们最开始 $O(n)$ 的做法中，时间复杂度的瓶颈在于最坏情况下要枚举整个 $[1, n]$ 区间判断，而判断完 **整个区间** 需要枚举 $n$ 次。 	我们发现要想找到答案，一定需要不重不漏的判断区间。那么是否存在一种可以加快这个过程的办法呢？

​	注意到题目给了我们一个很重要的信息：数组是单调不减的。假如数组的第 $i$ 个元素 $a_i$ **小于** $x$，则 $a_i$ 前面的 $i - 1$ 个数也一定小于 	$x$；对应地，如果 $a_i$ **大于等于** $x$，那么 $a_i$ 后面的 $n - i$ 个数也一定大于等于 $x$。这个性质叫做数的 **单调性**。

​	设我们要判断的区间是 $[l, r]$，令 $mid = \lfloor \frac{l + r}{2} \rfloor$，我们可以根据 $a_{mid}$ 的单调性来缩小下次判断的区间。如果当前位置大于等于 $x$，说	明 $mid$ 以及后面所有的数字也大于等于 $x$，此时如果当前位置是 **等于** 的，我们先更新一下答案。由于我们此时无法确定当前位置是	否是 **第一次出现** 的位置，所以还需要往前搜索。所以无论是大于还是等于，我们都没有必要往后面看了，直接令 $r = mid - 1$，此	时我们就缩小了一半的区间；否则，如果当前位置小于 $x$，说明 $mid$ 以及前面的位置也小于 $x$，我们也没有必要在前面搜索了，所	以令 $l = mid + 1$。此时又缩小了一半区间。直到最后，一定会出现 $l > r$ 的情况，说明我们已经把整个区间都扫完了。在这个过程	中，如果 $ans$ 被更新，它一定是不断往前走的，因此最后一次找到的 $ans$ 就是数组中最靠前的位置；如果从始至终都没有更新 $ans$	，说明数组中不存在这个数组，应该输出 $-1$。（上面的过程一定要画图模拟）。

​	由于我们每次操作都会让区间范围小 $\frac{1}{2}$，所以总时间复杂度是 $O(\log n)$的，这种操作就叫做二分查找。

​	（下面给出闭区间二分的模版）

```cpp
int l = 1, r = n, ans = -1;
while (l <= r){
  int mid = (l + r) / 2;
  if (a[mid] >= x) {
    if (a[mid] == x) ans = mid;
    r = mid - 1;
  } else {
    l = mid + 1;
  }
}
```

2. 二分查找

​	思考二分查找的性质，当我们待查找的集合具有单调性时，我们就可以用这种方式快速的查找答案。下面给出几道例题。

​	A4020 A-B 数对（学生先看题思考两分钟）

​	首先注意到，本题只要求我们找到符合的答案的数量，和数的顺序无关，那么我们可以先对数组排序，使得数组具有单调性。

​	依旧先考虑最暴力的做法，原式 $A - B = C$ 中， $C$ 是常数，而 $A$ 和 $B$ 都是需要在数组中找的数字，所以我们需要两个循环粉别来	找 $A$ 和 $B$。

```cpp
for (int i = 1; i <= n; i++) {
  for (int j = 1; j <= n; j++) {
    if (a[i] - a[j] == c) ans++;
  }
}
```

​	换一个思路，对于每一个 $A$，它对应的 $B$ 都是固定的。我们可以把式子变形为 $A - C = B$。此时问题就转化为了：对于每一个 $A$，	我们要快速的找出对应的 $B$ 的数量。

​	可以使用两个二分，第一次二分找数组中第一个大于等于 $B$ 的元素的位置，第二次二分找数组中第一个大于 $B$ 的元素的位置，用第	二个位置减第一个位置就是符合条件的 $B$ 的数量。

​	在这个过程中，枚举每一个 $A$ 是必要的，每个 $A$ 都要二分两次，所以时间复杂度是 $\Theta(2 n \log n)$，我们一般记作 $O(n \log n)$。

​	事实上，这种枚举两个未知数找合法对数的问题，我们都可以尝试将其转化为枚举第一个未知数，找第二个合法未知数数量的问题。

​	（先让学生尝试实现两个二分）。

​	在上面的问题中，我们需要实现两个二分，一个是找数组中第一个**大于等于**某个数的位置，另一个数找数组中第一个 **大于** 某个数的	位置。事实上在 C++ 的 stl 中有预先准备好的二分函数，分别为 std::lower_bound() 找第一个大于等于的位置 和std::upper_bound() 找第一个大于的位置。

​	接下来用这两个方法重新做一下上面的问题。

​	ABC077C（学生自己做）

​	ABC143D（学生自己做）	

​	ABC130D (略难，需要会前缀和)

3. 二分答案

   二分查找不仅可以用于枚举区间，还可以用于枚举答案。

   对于一些问题，我们很难直接计算出来它的答案，但是我们可以容易地去验证某个答案对于这个问题来说合不合法。因此我们可以考虑枚举可能的答案，找到其中符合我们要求的那一个。如果这些答案满足单调性，我们就可以用二分的方式寻找。

   P1147 砍树

   在这个问题中，我们发现当锯片的高度越高，能获得的木材越少。考虑枚举锯片高度，如果锯片的高度是 $x$ 米的时候我们能获得 $M$ 的木材，那么高度低于 $x$ 时我们也一定能获得 $M$ 米的木材。我们希望在满足这个要求的情况下让锯片的高度尽可能高，所以更新答案后移动左端点；否则移动右端点。

   因此我们可以给出二分答案的代码框架：

   ```cpp
   int l = min, r = max, ans = -1; 	//这里的 l 和 r 就变成了答案可能所处的区间
   while (l <= r){
     int mid = (l + r) / 2;
     if (check(mid)) {
       r = mid - 1;
     } else {
       l = mid + 1;
     }
   }
   ```

   现在考虑问题的具体实现，首先是二分的端点，我们发现最低的情况就是将锯片放在 $0$ 米的高度，此时我们会获得所有的木材；最高的情况就是将锯片的比最高的树还高，此时我们会获得 $0$ 的木材。这两个极端情况就可以作为端点。接下来考虑 check 怎么实现。对于当前的高度 $mid$，如果一棵树的高度是 $h_i$，那么就可以砍下 $\max(h_i - mid, 0)$ 的木材。从 $1$ 到 $n$ 遍历每棵树，记录能够砍下来的木材 $cnt$，那么符合条件的情况就是 $cnt \geq M$。

   每次 $check$ 的时间复杂度显然是 $O(n)$，所以总时间复杂度是 $O(n \log n)$。

   TW3026 跳石头（学生自己做）

   C1044 木材加工（学生自己做）

   ABC146C

   ABC312C （考虑上下界）

4. 浮点二分

   二分不仅可以用于整数域，还可以用于实数域。在计算机中，因为精度问题，一些涉及浮点数的题目我们无法得到准确的答案。此时出题人就会规定我们输出的答案与标准答案的误差在一定程度内就算正确。此时，我们也可以通过二分的方式不断的逼近答案区间。

   P1146 数的三次方根

   在本题中，题目要求我们保留六位小数，也就是说，我们的答案精度只要大于六位小数就可以了。

   下面给出本题浮点二分的写法：

   ```cpp
   while (r - l >= EPS) {	// eps 就是一个问题的精度限制，在本题中，我们的精度要大于六位小数，所以设EPS = 1e-7
     double mid = (l + r) / 2;
     if (mid * mid * mid >= n) {
       ans = mid;
       r = mid;
     } else {
       l = mid;
     }
   }
   ```

   同样，浮点二分也是可以做二分答案题目的。

   P1506 绳子（学生自己做）

1. 作业

​	ABC248D （在二维vector上二分）

​	ABC119D   （分讨，略微繁琐）

​	ABC212C

​	ABC174E

​	LG1180

​	LG1183

​	P1462（难）

6. 后记

​	Stop learning useless algorithms, go and solve some problems, learn how to use binary search.

​																				—— Um_nik