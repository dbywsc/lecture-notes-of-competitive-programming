1. DFS 的原理，DFS 解决走迷宫问题

   TW2112 走迷宫 

   由于每个格子只会被标记一次，所以时间复杂度为 $O(n^2)$

   和 BFS 相比，DFS 强调 “可达” ，不强调最短路。

2. 利用 DFS 可达任意点的性质，解决一些搜索状态的问题

   A5001 全排列问题

   DFS 的核心就是 可达性 + 回溯机制，因此思考 DFS 的状态设计时从下面几个角度考虑：

   - 初始的情况应该怎么表示？
   - 边界情况应该怎么处理？
   - 应该如何从当前状态走向下一步状态？
   - 何时需要回溯？

   类似于 BFS 中的八数码，如果将 DFS 的每个状态看成一个点，那么状态与状态之间的转移其实只是点与点之间的移动。是否回溯其实就是是否走回头路。

   拓展：设置场宽为 $x$ std::setw(x)

   ​			设置左对齐/右对齐	std::left / std::right

   ​			STL中的全排列：std::next_permutation()

   思考：解决这个问题的时间复杂度是多少？

   ​			这道题的时间复杂度其实就是全排列的数量，第一个位置有 $n$ 种选法，第二个位置有 $n - 1$ 种选法，第三个位置有 $n - 2$ 种			选法......

   ​			所以总共有 $n \times (n - 1) \times (n - 2) \times \cdots \times 2 \times 1 = n!$ 种选法，时间复杂度即为 $O(n!)$。

3. 练习：

   A5002 组合的输出（课上讲解）

   依旧从四个角度考虑这个问题，它和上道题的区别是什么？

   C1033 自然数的拆分 （20分钟，学生自行解决） $O(\operatorname p(n) \times L)$ 其中 $\operatorname P(n)$ 是 $n$ 的拆分数，$L$ 是平均解的长度。具体不深入考虑。

   总结，如何从四个角度思考？

   A2022 烤鸡 （20分钟，学生自行解决） $O(3^{10})$

   总结，在 DFS 的基础上，本题还加入了存储每个方案的要求。如何估算方案数的上限？

   上面两个问题的时间复杂度是多少？

4. 典题选讲

   A5007 八皇后

   如果直接对一个二维地图做操作，每次放一个棋子需要考虑到枚举其所在的行、列、对角线上的 格子，非常繁琐。

   考虑用打标记的方法解决这个问题。为列、主对角线、副对角线都建立一个数组。现在 DFS 每一行，对于当前行的每一列，能放棋子的前提就是所在的列、主对角线、副对角线都没有棋子。相应的，如果放了棋子，就要在对应的列、主副对角线做标记。

   思考：如何标记一个棋子所在的主、副对角线？

   主对角线：行 + 列；	副对角线：行 - 列，有可能是负数，所以 + n 映射。

   考虑几个问题：

     1. 如果是先输出方案数再输出前三个方案，应该怎么做？

     2. 这个问题的时间复杂度是多少？为什么在这个时间复杂度下还能够解决问题？ $O(n!)$  因为加入了**剪枝**操作。

     3. 什么是 NP？

        如果一个问题在 **多项式时间复杂度（Polynomial Time）** 下解决，我们称之为其是容易的。比如说 $O(m + n^2)$， $O(Tn^4)$ 都是多项式时间复杂度。反之我们称之为其是困难的，比如说 $O(n!)$， $O(2^n)$。

        如果一个**决策类**问题容易解决并且容易验证，我们称之为 **P**；

        如果一个**决策类**问题**不一定容易解决**，但是容易验证解的正确性，我们称之为**NP**；

        计算一个 **NP** 问题的解的个数的计数问题，我们称之为 **#P**；

        如果一个 **NP** 问题非常困难，解决它就能解决其他所有 **NP** 问题，我们称之为 **NP-complete**；

        如果一个问题难以解决，并且不一定容易验证，我们称之为 **NP-Hard**，其中，所有 **NP** 问题都可以归属到 **NP-Hard** 中，但是 **NP-Hard** 不一定是 **NP** 问题；

        如果一个 **#P** 问题非常困难，解决它就能解决其他所有 **#P** 问题，我们称之为 **#P-complete**；

        本题是一个 #P-complete 问题。

5. DFS 的剪枝优化

   A5017 小猫爬山

   暴力做法显然是暴力枚举不同小猫分配到不同缆车的情况，由于 $n$ 个小猫都有 $n!$ 种不同的方案，所以总时间复杂度是 $O(n \times n!)$。

   考虑剪枝，我们发现对于当前的方案，不管有没有结束，如果目前的数量已经**大于等于**之前所有方案的最小值就直接退出，这个过程叫做**最优性剪枝**。

   YL1076D8 空调温度（学生先思考五分钟）

   本题的转移有三种：加一，减二，乘二。可以发现本题中最优性剪枝依旧是适用的。

   对于当前状态大于目标状态的情况，可以发现没有必要做减二和加一的搜索操作，因为从当前温度到目标温度到操作数是可以直接算出来的。所以一旦遇到这种情况可以 $O(1)$ 的计算结果然后返回。

   因此，本题转化为了只需要做向上的搜索，这个时候状态显然是有限的。（提问：共有多少种状态？）

6. 记忆化搜索

   TW2014 斐波那契数列

   不严谨的说，递归求斐波那契数列的方式也是一种 DFS。

   然而，这种做法的时间复杂度近似 $O(2^n)$ ，显然是无法通过所有数据的。

   考虑斐波那契的计算过程，计算 $f(5)$ 需要先计算 $f(4)$ 和 $f(3)$，计算 $f(6)$ 又需要计算 $f(5)$ 和 $f(4)$ $\cdots$。可以发现大部分时间里，我们都是在做重复的计算。

   既然这些计算的结果是重复使用的，那我们为什么不一边计算一边存储结果呢？

   这样每个数都只被计算一次，时间复杂度就做到了近似 $O(n)$。

   A5005 滑雪（学生自己做20分钟）

   朴素做法就是从起点出发，往四个方向枚举，找比自己小的然后转移，要想找最长的长度就对每个格子都做一次 DFS。时间复杂度 $O(nm \times 4^{nm})$。

   注意到，对于某个格子 $x$，如果存在一个可以一步到达它切比它高的格子 $y$，那么 $y$ 的距离就相当于是 $x$ 的距离 $+1$。因此可以采取记忆化的策略，这样可以保证每个格子只被走一次，时间复杂度为 $O(nm)$。

   实际上，记忆化的思想与我们之后学习到的**动态规划**，这种可以用于后面状态的重复计算的性质，我们称之为**重叠子问题**。

7. 作业：

   P1114 跳台阶

   P1115 走方格

   YL1023B4 一维分型图

   A5006 棋盘问题

   A3119 字母组合

   A5022 靶型数独
